import React from 'react';

import './profileHeader.scss';

const ProfileHeader = () => {
  return (
    <div className="profile-header">
      <div className="profile-header__wrapper">My Account</div>
    </div>
  );
};

export default ProfileHeader;
