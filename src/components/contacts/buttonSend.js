import React from "react";
import "./style.scss";

const Send = () => {
  return(
    <div>
     <input type="submit" defaultValue="Send" className="button-send" />
    </div>
  );
    
};

export default Send;